# Generated by saltstack
client = yes

{% for tunnel_key, tunnel in salt['pillar.get']('stunnel')['client_tunnel'].items() %}
[{{ tunnel_key }}]
accept = {{ tunnel['accept'] }}
connect = {{ tunnel['connect'] }}
{% endfor %}
